export enum EnumRequestStatus {
  LOADING = 'LOADING',
  SUCCEED = 'SUCCEED',
  FAILED = 'FAILED'
}

export enum EnumGenderType {
  MALE = 1,
  FEMALE = 2
}

export enum EnumStatusType {
  ACTIVE = 1,
  INACTIVE = 2
}
